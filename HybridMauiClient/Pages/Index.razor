@layout MainLayout
@using HybridMauiClient.Auth;
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Authorization

@inject AuthenticationStateProvider AuthenticationStateProvider
@page "/"
<h1>Main - Index</h1>
@code {
    protected async override Task OnAfterRenderAsync(bool firstRender)
    {
        // var test = (ExternalAuthStateProvider)AuthenticationStateProvider;
        // var result = await test.LoginWithExternalProviderAsync();
        await ((ExternalAuthStateProvider)AuthenticationStateProvider)
            .LogInAsync();

    }
}